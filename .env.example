# Platform-level env for docker compose (read automatically from the project root)

# Unified gateway public port (nginx)
AODT_GATEWAY_PUBLIC_PORT=8082

# Note: In unified single-port mode, AODT-Agent is not exposed directly.
# If you run AODT-Agent standalone (using AODT-Agent/docker-compose.yml), use:
# AODT_AGENT_PUBLIC_PORT=8082
